<h1> Login </h1>

<%= form_for @user, controller: 'users', action: 'login' do |f| %>

  <% if @user.errors.any? %>
    <h3> Errors: </h3>
    <ul>
      <% @user.errors.full_messages.each do |message|  %>
        <li> <%= message %> </li>
      <% end %>
    </ul>
  <% end %>

  <%= f.label :username %>
  <%= f.text_field :username %><br>

  <%= f.label :password %>
  <%= f.password_field :password %><br>

  <%= f.submit "Login" %>
<% end %>